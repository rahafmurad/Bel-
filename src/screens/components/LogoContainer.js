import { View, Text } from 'react-native'
import styles from "../style/Style"

const LogoContainer = ()=>{
    return(
<View style={[styles.tabView, {
    backgroundColor: "#E4DAB6",
    width: 327,
    height: 60,
    borderRadius: 30,
    opacity: 0.75
  }]}>
  </View> );

};
export default LogoContainer;